{
  "name": "@altos/cloud",
  "version": "0.1.0",
  "type": "module",
  "description": "Altos cloud infrastructure - remote execution, sync, collaboration",
  "license": "Apache-2.0",
  "exports": {
    ".": "./dist/index.js",
    "./client": "./dist/client/index.js",
    "./worker": "./dist/worker/index.js",
    "./sync": "./dist/sync/index.js"
  },
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "dev": "tsc --build --watch",
    "test": "vitest run",
    "lint": "biome lint .",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
