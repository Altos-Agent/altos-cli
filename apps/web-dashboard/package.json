{
  "name": "@altos/web-dashboard",
  "version": "0.1.0",
  "type": "module",
  "description": "Altos web dashboard for monitoring and configuration",
  "license": "Apache-2.0",
  "private": true,
  "scripts": {
    "build": "tsc --build",
    "dev": "vite",
    "preview": "vite preview",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "lint": "biome lint .",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "typescript": "^5.5.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.8"
  }
}
